import { css } from 'styled-components';

const Icon = css`
color: white;
padding: 2px;
border-radius: 50%;

&:active, &:hover {
    cursor: pointer;
    background-color: #323232;
}`;

export default Icon;