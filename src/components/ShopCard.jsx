import React from 'react';
import Card from '../components/Card';

export default function ShopCard(props) {
    const { data } = props;
    console.log(data);

    return (
        <Card>
            
        </Card>
    )
}
